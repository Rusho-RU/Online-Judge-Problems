#include <bits/stdc++.h>
using namespace std;

vector<long long>dif;

void(long long n){
    for(int i=0;i<dif.size();i++){
        if(dif[i])
    }
}

int main(){
    long long n,l;
    while(scanf("%I64d %I64d",&n,&l)==2){
        long long value;
        vector<long long>val;
        scanf("%I64d",&value);
        for(int i=1;i<n;i++){
            scanf("%I64d",&value);
            dif.push_back(value);
        }

        double low=1,high=l,mid=(low+high)/2;
        while(low<high){

        }
    }
}
