#include <bits/stdc++.h>
using namespace std;

#define FasterIO ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0)

int main(){
    FasterIO;
    int n;

    while(cin>>n){
        int data[n], mx = INT_MIN, sum=0;

        for(int i=0; i<n; i++){
            cin>>data[i];
            sum+=data[i];

        }
    }
}

