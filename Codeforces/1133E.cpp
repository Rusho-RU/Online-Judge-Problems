#include <bits/stdc++.h>
using namespace std;

#define FasterIO ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0)
#define all(v) v.begin(), v.end()

int main(){
    FasterIO;

    int n, k;

    while(cin>>n>>k){
        long long data[n];

        for(int i=0; i<n; i++)
            cin>>data[i];
    }

    return 0;
}

