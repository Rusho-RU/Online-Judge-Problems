#include <bits/stdc++.h>
using namespace std;

int main(){
    int t, Case=0;
    scanf("%d",&t);

    while(t--){
        double ab, ac, bc, ratio;
        scanf("%lf %lf %lf %lf",&ab, &ac, &bc, &ratio);


    }
}
