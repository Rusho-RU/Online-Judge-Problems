#include <bits/stdc++.h>
using namespace std;

#define MAX 1000000000

int main(){
    int t;
    scanf("%d",&t);
    for(int Case=1;Case<=t;Case++){
        int node,edge,a,b,c;
        scanf("%d%d",&node,&edge);
        int data[500][500]={};
        for(int i=0;i<edge;i++){
            scanf("%d%d%d",&a,&b,&c);
            if(!data[a][b])
        }
    }
}
