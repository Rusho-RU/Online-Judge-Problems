#include <stdio.h>

int main(){
    while(1){
        int n,i,j;
        scanf("%d",&n);
        if(n==0)
            break;
        char s[n][1002];
        int l[n],rem[n];
        for(i=0;i<n;i++){
            scanf("%s",s[i]);
            l[i]=strlen(s[i]);
        }
        for(i=0;i<n;i++){
            for(j=0;j<n;j++){

            }
        }
    }
}
